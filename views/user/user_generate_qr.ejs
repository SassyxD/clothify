<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ชำระเงินด้วย PromptPay</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/user/user_login.css">
    <style>
        .navbar-custom {
            background-color: #1e1e1e;
            border-bottom: 1px solid #333;
        }

        .navbar-custom .nav-link {
            color: #fff;
        }

        .navbar-custom .nav-link:hover {
            color: #d4a373;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-custom">
        <div class="container-fluid">
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/user_log_out">ออกจากระบบ</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/user_history">ประวัติการนัด</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/user_homepage">หน้าหลัก</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container mt-5">
        <div class="card">
            <h1>สแกน QR Code เพื่อชำระเงิน</h1>
            <img src="<%= qrCode %>" alt="PromptPay QR Code">
            <form action="/payment-success" method="POST">
                <input type="hidden" name="date" value="<%= date %>">
                <input type="hidden" name="service" value="<%= service %>">
                <input type="hidden" name="time" value="<%= time %>">
                <button type="submit" class="btn">ชำระเงินเสร็จสิ้น</button>
            </form>
        </div>
    </div>

</body>

</html>