<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ชำระเงินด้วย PromptPay</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/user/user_login.css">
    <link rel="stylesheet" href="/css/user/nav_login.css">
    <style>
        .navbar-custom {
            background-color: #1e1e1e;
            border-bottom: 1px solid #333;
        }

        .navbar-custom .nav-link {
            color: #fff;
        }

        .navbar-custom .nav-link:hover {
            color: #d4a373;
        }
    </style>
</head>

<body>
    <%- include('navbar_login') %>
    <div class="container" style="margin-top: 8rem;">
        <div class="card">
            <h2>สแกน QR Code เพื่อชำระเงินค่าธรรมเนียมจอง</h2>
            <p>500.00 บาท</p>
            <img src="<%= qrCode %>" alt="PromptPay QR Code">
            <form action="/payment_success" method="POST">
                <input type="hidden" name="date" value="<%= date %>">
                <input type="hidden" name="service" value="<%= service %>">
                <input type="hidden" name="time" value="<%= time %>">
                <button type="submit" class="btn">ชำระเงินเสร็จสิ้น</button>
            </form>
        </div>
    </div>

</body>

</html>