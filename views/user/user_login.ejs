<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ลงชื่อเข้าใช้</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/user/user_login.css">
</head>
<body>
    <div class="bar">
        <div class="logo">Clothify</div>
        <a href="/">หน้าหลัก</a>
    </div>
    <div class="container">
        <div class="card">
            <h3>ลงชื่อเข้าใช้</h3>
            <% if (typeof success !== 'undefined') { %>
                <% if (success === 'true') { %>
                    <div style="color: green;">
                        สมัครบัญชีเสร็จสิ้น! กรุณาเข้าสู่ระบบ
                    </div>
                <% } else if (success === 'not_found') { %>
                    <div style="color: red;">
                        ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง
                    </div>
                <% } else if (success === 'pleaselogin') { %>
                    <div style="color: red;">
                        กรุณาเข้าสู่ระบบ
                    </div>
                <% } %>
            <% } %>
            <form action="/user_login_action" method="POST">
                <div class="mb-3 text-start">
                    <label for="username" class="form-label">ชื่อบัญชื่อผู้ใช้</label>
                    <input type="text" id="username" name="username" class="form-control" required><br><br>
                </div>
            
                <div class="mb-3 text-start">
                    <label for="password" class="form-label">รหัสผ่าน</label>
                    <input type="password" id="password" name="password" class="form-control" required><br><br>
                </div>
                <p style="color: white;">ยังไม่บัญชี? <a href="/user_register" >สมัครสมาชิก</a></p>
                <button type="submit" class="btn">ยืนยัน</button>
            </form>
        </div>
    </div>
</body>
</html>