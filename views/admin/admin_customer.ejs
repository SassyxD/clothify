<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ข้อมูลลูกค้า</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin/nav_admin.css">
    <style>
        .container-fluid{
            margin-top: 8rem;
            padding-right: 20px;
            padding-left: 20px;
        }

        .container-fluid::before{
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-image: url('/image/blacksuit_bg.png'); 
            background-size: cover; 
            background-position: center;
            filter: brightness(30%);
            z-index: -1;
            height: 100%;
            background-attachment: fixed;
        }
    </style>
</head>

<body>
    <%- include('admin_nav') %>
        <div class="container-fluid">
            <h1 style="color: antiquewhite;">ข้อมูลลูกค้า</h1>
            <table class="table table-hover table-striped table-bordered table-dark">
                <thead>
                    <tr>
                        <th>รหัสลูกค้า</th>
                        <th>ชื่อ</th>
                        <th>นามสกุล</th>
                        <th>เบอร์โทรศัพท์</th>
                        <th>อีเมล</th>
                        <th>การดำเนินการ</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (customers.length> 0) { %>
                        <% customers.forEach((customer)=> { %>
                            <tr>
                                <td>
                                    <%= customer.CustomerID %>
                                </td>
                                <td>
                                    <%= customer.FirstName %>
                                </td>
                                <td>
                                    <%= customer.LastName %>
                                </td>
                                <td>
                                    <%= customer.PhoneNumber %>
                                </td>
                                <td>
                                    <%= customer.Email %>
                                </td>
                                <td>
                                    <!-- ปุ่มดูประวัติการนัด -->
                                    <a href="/admin/customer_appointments/<%= customer.CustomerID %>" class="btn btn-info btn-sm">ดูประวัติการนัด</a>
                                </td>
                            </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td colspan="6" style="text-align: center; font-size: 24px;">ยังไม่มีข้อมูล</td>
                            </tr>
                            <% } %>
                </tbody>
            </table>
        </div>
</body>

</html>