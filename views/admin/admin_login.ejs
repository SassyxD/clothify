<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ลงชื่อเข้าใช้ (Admin)</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/user/user_login.css">
</head>

<body>
    <div class="bar">
        <a href="/" class="logo">Clothify</a>
    </div>
    <div class="container">
        <div class="card">
            <h3>ลงชื่อเข้าใช้ผู้จัดการ</h3>
            <form action="/admin_login_action" method="POST">

                <div class="mb-3 text-start">
                    <label for="username" class="form-label">ชื่อบัญชื่อผู้ใช้</label>
                    <input type="text" id="username" name="username" placeholder="ชื่อบัญชื่อผู้ใช้"
                        class="form-control" required><br><br>
                </div>

                <div class="mb-3 text-start">
                    <label for="password" class="form-label">รหัสผ่าน</label>
                    <input type="password" id="password" name="password" placeholder="รหัสผ่าน" class="form-control"
                        required><br><br>
                </div>
                <button type="submit" class="btn">ยืนยัน</button>
            </form>
            <% if (typeof error !=='undefined' ) { %>
                <% if (error==='user_not_found' ) { %>
                    <p style="color: red;">ไม่มีบัญชีในระบบ</p>
                    <% } else if (error==='invalid_credentials' ) { %>
                        <p style="color: red;">บัญชีหรือรหัสผ่านผิด</p>
                        <% } %>
                            <% } %>
        </div>
    </div>
</body>

</html>